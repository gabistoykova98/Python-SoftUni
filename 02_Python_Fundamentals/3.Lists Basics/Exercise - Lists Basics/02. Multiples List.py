num_one = int(input())
num_two = int(input())
# gabi = []
#
# for robi in range(num_one, num_one * num_two + 1, num_one):
#     gabi.append(robi)

print([num for num in range(num_one, num_one * num_two +1, num_one)])

